# Enable AndroidX libraries required by the application dependencies.
android.useAndroidX=true
# Migrate legacy support-library dependencies when encountered.
android.enableJetifier=true
# Keep Gradle memory stable on CI builders.
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
